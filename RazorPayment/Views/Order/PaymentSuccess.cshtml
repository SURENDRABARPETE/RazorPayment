@model OrderEntity


<div class="container">
    <div class="card">
        <div class="card-body">
            <div class="row">
                <label> payment Received  Success</label>
            </div>
            <div class="row">

                <div class="form-group">
                    <lable>Transection Number</lable>
                    <span>@Html.DisplayText("TransectionID")</span>
                </div>
                <div class="form-group">
                    <lable>Order Number</lable>
                    <span>@Html.DisplayText("OrderID")</span>
                </div>
            </div>
        </div>
    </div>

</div>